import theme from 'mdx-deck/themes'

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: 'Josefin Sans, Roboto, sans-serif',
  // custom colors
  colors: {
    text: '#F8F8F2',
    background: '#282A36',
    link: '#FF79C6',
  }
}